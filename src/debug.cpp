//---------------------------------------------------------------------------
#include "prfconst.h"


#include <stdio.h>
#include "debug.h"
//---------------------------------------------------------------------------
void debugprint (const char *sinfo) {
  (void)(sinfo);
/*
    FILE *out;
    if ( (out = fopen("prf.deb","at")) !=NULL )
        fprintf(out,"%s\n",sinfo);
    fclose(out);
*/
}
//---------------------------------------------------------------------------
